const config = {
  appName: '课时管理系统',
  // 线上后端地址（HTTPS 域名）
  // 本地开发时可改回：http://127.0.0.1:8001
  // 注意：手机上的 127.0.0.1 指的是手机自己，不是电脑。
  baseURL: 'http://127.0.0.1:8000',
  useMock: false,
  requestTimeout: 12000
}

export default config
